# iOS application (planned)

The first milestone is Android-only. The repository layout deliberately keeps room for a future Swift + SwiftUI app using the shared native AI core.

Android-only mechanisms such as APK self-update will not be reused on iOS; iOS distribution and updates will use Apple-supported channels.
